---
import HeroBanner from "../components/HeroBanner.astro";
import AppLayout from "../layouts/AppLayout.astro";

const title = "Skills";
const workSampleLinkDimension = "var(--sizing-11)";
---

<AppLayout title={title}>
  <HeroBanner
    patternSrc="url('/images/patterns/topography.svg')"
    subtitle={title}
    title="Anna Borja"
  />

  <div
    class="content-container-outer flexbox-vertical with-responsive-padding-inline"
  >
    <div class="content-container-inner flexbox-vertical">
      <div class="flexbox-vertical flexbox-vertical-paragraphs">
        <p>
          If you want to see my game design skills in context, I suggest heading
          over to the <a class="text-link" href="/projects">Projects</a> page. However,
          if you’re also interested in the wider variety of creative knowledge and
          experience that I could bring to a team, then you’re in the right place.
        </p>
      </div>

      <div class="flexbox-vertical sections-container">
        <section class="flexbox-vertical section">
          <div>
            <h1 class="section-title">Writing</h1>

            <div class="flexbox-vertical flexbox-vertical-paragraphs">
              <p>
                My favorite examples of video game writing not only contribute
                to the player’s immersion in the world but also further the
                development of the game’s story, characters, or themes.
              </p>
            </div>
          </div>

          <div>
            <h2 class="section-subheading">Writing Samples</h2>

            <ul class="work-samples-list">
              <li>Foo</li>
              <li>Bar</li>
            </ul>
          </div>
        </section>

        <section class="section">
          <h1 class="section-title">Art</h1>
        </section>

        <section class="section">
          <h1 class="section-title">Programming</h1>
        </section>

        <section class="section">
          <h1 class="section-title">Cinematics</h1>
        </section>
      </div>
    </div>
  </div>
</AppLayout>

<style define:vars={{ workSampleLinkDimension }}>
  .content-container-inner {
    gap: var(--spacing-8);
  }

  .content-container-outer {
    align-items: center;
    padding-block: var(--spacing-7);
  }

  .section {
    gap: var(--spacing-7);
  }

  .section-subheading {
    margin-block-end: var(--spacing-3);
  }

  .section-title {
    margin-block-end: var(--spacing-3);
    text-align: center;
  }

  .sections-container {
    gap: var(--spacing-9);
  }

  .work-samples-list {
    flex-direction: row;
    flex-wrap: wrap;
    gap: var(--spacing-5);
  }

  .work-samples-list li {
    background-color: #ddd;
    border-radius: var(--border-radius-1);
    height: var(--workSampleLinkDimension);
    padding: var(--spacing-4);
    width: var(--workSampleLinkDimension);
  }

  @media (min-width: 768px) {
    .content-container-inner {
      max-inline-size: var(--sizing-16);
    }
  }
</style>
