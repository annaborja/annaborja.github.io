{
  "plugins": ["@emotion"],
  "presets": [
    ["@babel/preset-env", { "targets": "defaults" }],
    [
      "@babel/preset-react",
      {
        "importSource": "@emotion/react",
        "runtime": "automatic"
      }
    ],
    "@babel/preset-typescript"
  ]
}
